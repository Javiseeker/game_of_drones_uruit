import { TextboxValidatorDirective } from './textbox-validator.directive';

describe('TextboxValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TextboxValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
