import { DropdownValidatorDirective } from './dropdown-validator.directive';

describe('DropdownValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
